== input ==
public class Test {
    void test() {
        int max = a > b ? a : b;
        String msg = error ? "failed" : "success";
    }
}
== output ==
public class Test {

    void test() {
        int max = a > b ? a : b;
        String msg = error ? "failed" : "success";
    }
}
