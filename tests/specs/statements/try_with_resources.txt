== input ==
public class Test {
    void test() {
        try (FileInputStream fis = new FileInputStream("file.txt")) {
            readData(fis);
        } catch (IOException e) {
            handleError(e);
        }
    }
}
== output ==
public class Test {

    void test() {
        try (FileInputStream fis = new FileInputStream("file.txt")) {
            readData(fis);
        } catch (IOException e) {
            handleError(e);
        }
    }
}
