== input ==
if (error.isPresent()) {
    throw error.get();
}
// check credentials
Optional<Credentials> credentials = credentials(context.securitySource());
== output ==
if (error.isPresent()) {
    throw error.get();
}

// check credentials
Optional<Credentials> credentials = credentials(context.securitySource());
