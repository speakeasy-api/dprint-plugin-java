== input ==
public class Test {
    public <T> T getFirst(List<T> list) {
        return list.get(0);
    }

    public <K, V> Map<K, V> createMap() {
        return new HashMap<>();
    }
}
== output ==
public class Test {

    public <T> T getFirst(List<T> list) {
        return list.get(0);
    }

    public <K, V> Map<K, V> createMap() {
        return new HashMap<>();
    }
}
